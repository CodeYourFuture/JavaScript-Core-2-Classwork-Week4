function printGreeting() {
  const firstName = "Moussab";
}

console.log(`Hello there, ${firstName}`);

printGreeting();

// first name is not defined and variable is defined inside the function (scope function)
