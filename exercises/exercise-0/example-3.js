function printGreeting() {
  const firstName = "Moussab";
}

console.log(`Hello there, ${firstName}`);

printGreeting();

// It won't be implemented correctly because firsly the fifth line will be executed and firsName still do not declared and also firsName is defined into function and we can not access this variable outside printGreeting's function.
